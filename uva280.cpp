#include<bits/stdc++.h>
#define sz 1005
using namespace std;

vector <int> vec[200];
bool reachable [100][100];
int main()
{
    int a,b,m,n;
    while(true)
    {
        cin>>m;

        if(m==0)
        {
            break;
        }
        cin>>n;
        for(i=0;i<n;i++)
        {
            cin>>a>>b;
            vec[a].push[b];


        }
    }
}

